# 关于可视化的pyOpenGL模版（和离散微分几何无关的一些知识）

“00-固定颜色代码模版.py”是一个固定颜色的、用来显示一个mesh的代码模版，使用pyOpenGL编程管线的技术。效果如下，左键旋转模型，右键旋转光照，滚轮调节远近，w键切换网格模式还是常规模式：

![图片](https://pic1.zhimg.com/v2-4c044658a13f30dd01159c0480843044.jpg)

![图片](https://pic2.zhimg.com/v2-5af9b6ecdd66c166ddf0074b5357e733.jpg)

![图片](https://pic2.zhimg.com/v2-8f38312eeae492b8a7fdee29d8d71683.jpg)

那么这是把坐标、法线放进了VBO写出来的。各种旋转、投影变换不多说了和这里无关，两个着色器是这样的可以贴一下（其实和这里也无关）：

```glsl
VERTEX_SHADER = """
#version 330
layout(location = 0) in vec3 position;
layout(location = 1) in vec3 normal;

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

out vec3 FragPos;
out vec3 Normal;

void main() {
    FragPos = vec3(model * vec4(position, 1.0));
    Normal = mat3(transpose(inverse(model))) * normal;
    gl_Position = projection * view * vec4(FragPos, 1.0);
}
"""

FRAGMENT_SHADER = """
#version 330
in vec3 FragPos;
in vec3 Normal;

out vec4 color;

uniform vec3 lightDir;
uniform vec3 objectColor;

void main() {
    vec3 norm = normalize(Normal);
    float diff = max(dot(norm, normalize(lightDir)), 0.0);
    vec3 diffuse = diff * objectColor;
    vec3 ambient = 0.2 * objectColor;
    color = vec4(diffuse + ambient, 1.0);
}
"""
```

objectColor是一个uniform量，和CPU上保持一致，每帧要传入的。

那么也可以是彩色的，在“01-彩色的代码模版.py”有一个模版，我这里上的色是下一节要讲到的，角亏，也叫做离散高斯曲率。

![图片](https://pica.zhimg.com/v2-7da4930a5a6984e20e5d6e7dde973fb4.jpg)

![图片](https://pic3.zhimg.com/v2-790a2c61206a18c0181bbb52dd2e705c.jpg)

![图片](https://pica.zhimg.com/v2-b33a1bd733eb19e484282e076993dc18.jpg)

这样准备数据的时候就有三大列：

```python
data = []
for f in faces:
    n = get_normal(verts, *f)
    for idx in f:
        v = verts[idx]
        c = colors[idx]
        data.extend([*v, *n, *c])

data = np.array(data, dtype=np.float32)
```

着色器代码也有相应调整：

```glsl
VERTEX_SHADER = """
#version 330
layout(location = 0) in vec3 position;
layout(location = 1) in vec3 normal;
layout(location = 2) in vec3 object_color;

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

out vec3 FragPos;
out vec3 Normal;
out vec3 ObjectColor;

void main() {
    FragPos = vec3(model * vec4(position, 1.0));
    Normal = mat3(transpose(inverse(model))) * normal;
    ObjectColor = object_color;
    gl_Position = projection * view * vec4(FragPos, 1.0);
}
"""

FRAGMENT_SHADER = """
#version 330
in vec3 FragPos;
in vec3 Normal;
in vec3 ObjectColor;

out vec4 color;

uniform vec3 lightDir;

void main() {
    vec3 norm = normalize(Normal);
    float diff = max(dot(norm, normalize(lightDir)), 0.0);
    vec3 diffuse = diff * ObjectColor;
    vec3 ambient = 0.2 * ObjectColor;
    color = vec4(diffuse + ambient, 1.0);
}
"""
```

当然也有别的选择，例如pyvista，这个我后来让ai写了写似乎也还不错，不过我没太使用。